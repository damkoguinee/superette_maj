<form method="post" action="{{ path('app_logescom_personnel_primes_personnel_delete', {'id': primes_personnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ primes_personnel.id) }}">
    <button class="btn">Delete</button>
</form>
