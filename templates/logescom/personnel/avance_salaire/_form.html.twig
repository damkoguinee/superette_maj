{{ form_errors(form) }}
{{ form_start(form) }}
    <div class="row mb-1"> 
        <div class="col-sm-12 col-md-4">
            {{ form_row(form.user) }}
        </div>               
        <div class="col-sm-12 col-md-2">
            {{ form_row(form.periode) }}
        </div>

        <div class="col-sm-12 col-md-6">
            <div class="row">
                <div class="col-sm-12 col-md-8">
                    {{ form_row(form.montant) }}
                </div>
                <div class="col-sm-12 col-md-4">
                    
                </div>
            </div>
        </div>

    <div>
    <div class="row mb-1">
        <div class="col-sm-12 col-md-2">{{ form_row(form.devise) }}</div>

        <div class="col-sm-12 col-md-3">
            {{ form_row(form.modePaiement) }}
        </div>
        <div class="col-sm-12 col-md-4">
            {{ form_row(form.details) }}
        </div>
        <div class="col-sm-12 col-md-3">
            {{ form_row(form.caisse) }}
        </div>
    </div>
    <button class="btn btn-primary my-4">{{ button_label|default('Valider') }}</button>
{{ form_end(form) }}
