<form method="post" action="{{ path('app_logescom_personnel_absences_personnels_delete', {'id': absences_personnel.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ absences_personnel.id) }}">
    <button class="btn">Delete</button>
</form>
