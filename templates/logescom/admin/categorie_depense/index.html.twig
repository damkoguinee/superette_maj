{% extends 'base.html.twig' %}

{% block title %}CategorieDepense index{% endblock %}

{% block body %}
    <h1>CategorieDepense index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Description</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for categorie_depense in categorie_depenses %}
            <tr>
                <td>{{ categorie_depense.id }}</td>
                <td>{{ categorie_depense.description }}</td>
                <td>
                    <a href="{{ path('app_logescom_admin_categorie_depense_show', {'id': categorie_depense.id}) }}">show</a>
                    <a href="{{ path('app_logescom_admin_categorie_depense_edit', {'id': categorie_depense.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_logescom_admin_categorie_depense_new') }}">Create new</a>
{% endblock %}
