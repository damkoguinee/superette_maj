<form method="post" action="{{ path('app_logescom_admin_frais_sup_delete', {'id': frais_sup.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ frais_sup.id) }}">
    <button class="btn">Delete</button>
</form>
