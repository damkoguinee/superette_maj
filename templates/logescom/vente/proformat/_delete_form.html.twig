<form method="post" action="{{ path('app_logescom_vente_proformat_delete', {'id': proformat.id, lieu_vente : lieu_vente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ proformat.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
