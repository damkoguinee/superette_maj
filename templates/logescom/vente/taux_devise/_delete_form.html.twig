<form method="post" action="{{ path('app_logescom_vente_taux_devise_delete', {'id': taux_devise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taux_devise.id) }}">
    <button class="btn">Delete</button>
</form>
