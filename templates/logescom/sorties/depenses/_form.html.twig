{{ form_errors(form) }}
{{ form_start(form) }}
    <div class="row  mb-1">
        <label class="form-label mx-1" for="">Catégorie dépense <a href="{{path('app_logescom_admin_categorie_depense_new', { lieu_vente : lieu_vente.id }) }}" class="btn btn-sm btn-warning"><i class="fas fa-plus"></i></a></label>
        <div class="mx-1">{{ form_widget(form.categorieDepense) }}</div>
    </div>
    <div class="row mb-1">
        {{ form_row(form.montant) }}
    </div>
    <div class="row mb-1">        
        <div class="col-sm-12 col-md-3">
            {{ form_row(form.tva) }}
        </div>

        <div class="col-sm-12 col-md-3">
            {{ form_row(form.devise) }}
        </div>

        <div class="col-sm-12 col-md-3">
            {{ form_row(form.modePaiement) }}
        </div>

        <div class="col-sm-12 col-md-3">
            {{ form_row(form.caisseRetrait) }}
        </div>
    </div>

    <div class="row mb-1">  
        <div class="col-sm-12 col-md-3">
            {{ form_row(form.dateDepense) }}
        </div>      
        <div class="col-sm-12 col-md-6">
            {{ form_row(form.description) }}
        </div>

        <div class="col-sm-12 col-md-3">
            {{ form_row(form.justificatif) }}
        </div>
    </div>
    <button class="btn btn-primary">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
