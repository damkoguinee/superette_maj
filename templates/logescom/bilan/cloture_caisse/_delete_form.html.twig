<form method="post" action="{{ path('app_nhema_caisse_cloture_caisse_delete', {'id': cloture_caisse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cloture_caisse.id) }}">
    <button class="btn">Delete</button>
</form>
