<form method="post" action="{{ path('app_logescom_caisse_transfert_fond_delete', {'id': transfert_fond.id, lieu_vente : lieu_vente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfert_fond.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
