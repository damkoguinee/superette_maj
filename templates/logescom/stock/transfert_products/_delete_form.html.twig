<form method="post" action="{{ path('app_logescom_stock_transfert_products_delete', {'id': transfert_product.id, lieu_vente : lieu_vente.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ transfert_product.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
