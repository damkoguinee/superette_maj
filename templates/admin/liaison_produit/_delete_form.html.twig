<form method="post" action="{{ path('app_admin_liaison_produit_delete', {'id': liaison_produit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liaison_produit.id) }}">
    <button class="btn">Delete</button>
</form>
