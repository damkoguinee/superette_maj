<form method="post" action="{{ path('app_admin_bannieres_entreprise_delete', {'id': bannieres_entreprise.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ bannieres_entreprise.id) }}">
    <button class="btn">Delete</button>
</form>
