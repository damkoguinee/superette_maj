<form method="post" action="{{ path('app_admin_liste_stock_delete', {'id': liste_stock.id}) }}" onsubmit="return confirm('Etes-vous sûr de vouloir supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ liste_stock.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>
