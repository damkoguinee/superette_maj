<form method="post" action="{{ path('app_admin_'~ nameEntite ~ '_delete', {'id': entite.id}) }}" onsubmit="return confirm('êtes-vous sûr de supprimer?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ entite.id) }}">
    <button class="btn btn-danger">Supprimer</button>
</form>